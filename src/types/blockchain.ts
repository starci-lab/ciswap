export enum ChainKey {
    Aptos = "aptos",
    Solana = "solana",
    Sui = "sui",
}

export enum PlatformKey {
    Aptos = "aptos",
    Solana = "solana",
    Sui = "sui",
    Evm = "evm",
}

export enum Network {
    Mainnet = "mainnet",
    Testnet = "testnet",
}